# Frontend example env file
# Set the API base URL used by the client. In development you can either use the
# Vite proxy (recommended) OR set this value to the backend address.
# If using the proxy you can leave this unset.

# VITE_API_BASE_URL=http://localhost:5001/api
